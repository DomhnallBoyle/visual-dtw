# Queue list ids for session selection every minute
* * * * * cd /opt/visual-dtw && env - `cat /container.env` python3 scripts/session_selection.py queue_list_ids >> /var/log/cron.log 2>&1
# This extra line makes it a valid cron